const noContent = "No content found";

const success = "Message successfully sent!";

const failedToRegister = "Failed to register!";

const accountCreated = "Account created!";

const unauthorized = "Account does not exist";

export {
  noContent,
  success,
  failedToRegister,
  accountCreated,
  unauthorized,
};
